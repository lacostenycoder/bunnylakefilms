<%= form_for([:admin, @work_status]) do |f| %>
  <% if @work_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_status.errors.count, "error") %> prohibited this work_status from being saved:</h2>

      <ul>
      <% @work_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
